import { BooleanHandlerPipe } from './boolean-handler.pipe';

describe('BooleanHandlerPipe', () => {
  it('create an instance', () => {
    const pipe = new BooleanHandlerPipe();
    expect(pipe).toBeTruthy();
  });
});
